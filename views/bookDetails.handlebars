cat > views/bookDetails.handlebars << 'EOF'
<style>
    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
        min-height: 100vh;
        margin: 0;
        padding: 0;
    }
    
    .book-details-container {
        background: white;
        border-radius: 20px;
        padding: 3rem;
        box-shadow: 0 20px 40px rgba(0,0,0,0.2);
        max-width: 900px;
        margin: 2rem auto;
    }
    
    .book-details-header {
        display: grid;
        grid-template-columns: 300px 1fr;
        gap: 3rem;
        margin-bottom: 2rem;
    }
    
    .book-details-image {
        width: 100%;
        height: 450px;
        object-fit: cover;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }
    
    h1 {
        color: #1f2937;
        font-size: 2.5rem;
        margin-bottom: 1rem;
    }
    
    .book-details-meta p {
        font-size: 1.1rem;
        margin: 1rem 0;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .book-details-meta strong {
        color: #764ba2;
        min-width: 100px;
    }
    
    .category-tag {
        background: #e0e7ff;
        color: #4338ca;
        padding: 0.25rem 0.75rem;
        border-radius: 20px;
        font-size: 0.9rem;
    }
    
    .price {
        color: #10b981;
        font-size: 2rem;
        font-weight: bold;
    }
    
    .add-to-cart {
        width: 100%;
        padding: 1rem;
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        color: white;
        border: none;
        border-radius: 10px;
        font-size: 1.2rem;
        font-weight: bold;
        curs