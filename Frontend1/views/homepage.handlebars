<style>
    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
        min-height: 100vh;
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
    }
    

    
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
    }
    
    .hero-section {
        text-align: center;
        padding: 3rem 0;
        margin-bottom: 3rem;
        position: relative;
    }
    
    .logo-title {
        font-size: 5rem;
        font-weight: 900;
        color: white;
        text-shadow: 
            3px 3px 0px rgba(0,0,0,0.2),
            6px 6px 20px rgba(0,0,0,0.3),
            0 0 60px rgba(255,255,255,0.4);
        margin: 0;
        letter-spacing: -2px;
        animation: glow 2s ease-in-out infinite alternate;
        display: inline-block;
        position: relative;
    }
    
    .book-icon {
        font-size: 4rem;
        animation: bounce 2s infinite;
        display: inline-block;
        margin-right: 1rem;
    }
    
    @keyframes glow {
        from {
            text-shadow: 
                3px 3px 0px rgba(0,0,0,0.2),
                6px 6px 20px rgba(0,0,0,0.3),
                0 0 40px rgba(255,255,255,0.4),
                0 0 60px rgba(255,255,255,0.2);
        }
        to {
            text-shadow: 
                3px 3px 0px rgba(0,0,0,0.2),
                6px 6px 25px rgba(0,0,0,0.4),
                0 0 50px rgba(255,255,255,0.6),
                0 0 80px rgba(255,255,255,0.4);
        }
    }
    
    @keyframes bounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
    }
    
    .subtitle {
        color: rgba(255,255,255,0.95);
        font-size: 1.5rem;
        margin-top: 1rem;
        font-weight: 300;
        letter-spacing: 2px;
        text-transform: uppercase;
        opacity: 0.9;
    }
    
    .decorative-line {
        height: 4px;
        background: linear-gradient(90deg, 
            transparent 0%, 
            rgba(255,255,255,0.8) 25%, 
            rgba(255,255,255,0.8) 75%, 
            transparent 100%);
        margin: 2rem auto;
        max-width: 300px;
        border-radius: 2px;
    }
    
    h2 {
        color: white !important;
        font-size: 2.5rem;
        margin: 3rem 0 1.5rem;
        padding-left: 1rem;
        border-left: 5px solid rgba(255,255,255,0.8);
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }
    
    #searchForm {
        background: rgba(255,255,255,0.98);
        padding: 2rem;
        border-radius: 20px;
        box-shadow: 
            0 20px 40px rgba(0,0,0,0.2),
            0 0 60px rgba(255,255,255,0.1);
        margin-bottom: 3rem;
        display: flex;
        gap: 1rem;
        backdrop-filter: blur(10px);
    }
    
    #searchInput {
        flex: 1;
        padding: 1.2rem;
        border: 3px solid #e5e7eb;
        border-radius: 12px;
        font-size: 1.1rem;
        transition: all 0.3s;
        background: rgba(255,255,255,0.9);
    }
    
    #searchInput:focus {
        outline: none;
        border-color: #667eea;
        box-shadow: 0 0 0 4px rgba(102,126,234,0.15);
        background: white;
    }
    
    button {
        padding: 1.2rem 3rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 1.1rem;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s;
        box-shadow: 0 6px 20px rgba(102,126,234,0.4);
        text-transform: uppercase;
        letter-spacing: 1px;
    }
    
    button:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(102,126,234,0.5);
    }
    
    .book-list {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 2.5rem;
        margin-bottom: 3rem;
    }
    
    .book-card {
        background: rgba(255,255,255,0.98);
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 
            0 15px 35px rgba(0,0,0,0.2),
            0 5px 15px rgba(0,0,0,0.1);
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        cursor: pointer;
        position: relative;
    }
    
    .book-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 5px;
        background: linear-gradient(90deg, #667eea, #764ba2, #667eea);
        background-size: 200% 100%;
        animation: shimmer 3s linear infinite;
    }
    
    @keyframes shimmer {
        0% { background-position: -200% 0; }
        100% { background-position: 200% 0; }
    }
    
    .book-card:hover {
        transform: translateY(-15px) scale(1.03);
        box-shadow: 
            0 25px 50px rgba(0,0,0,0.3),
            0 10px 20px rgba(0,0,0,0.2);
    }
    
    /* ============================================
     * MODIFIED BY: Michael Yeh - Aug 8, 2024
     * 
     * PURPOSE: Display complete book covers without cropping
     * 
     * CHANGES MADE:
     *   - Image height: 280px -> 200px (reduced to show full cover)
     *   - Object-fit: cover -> contain (prevent image cropping)  
     *   - Background: gradient -> #f8f9fa (light gray for consistency)
     *   - Added padding: 10px (create space around image)
     * 
     * ORIGINAL CODE:
     *   .book-card img {
     *       width: 100%;
     *       height: 280px;
     *       object-fit: cover;
     *       background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
     *   }
     * 
     * REASON: User feedback - book covers were being cut off,
     *         making it difficult to see complete book designs.
     *         Team requested full cover visibility for better UX.
     * 
     * TESTED: Chrome, Safari, Firefox - all covers display correctly
     * ============================================ */
    
    .book-card img {
        width: 100%;
        height: 200px;  /* MODIFIED by Michael: was 280px - reduced for complete visibility */
        object-fit: contain;  /* MODIFIED by Michael: was 'cover' - changed to 'contain' to show full image */
        background: #f8f9fa;  /* MODIFIED by Michael: was gradient - changed to solid light gray */
        padding: 10px;  /* ADDED by Michael: new property for spacing */
    }
    
    /* ============================================
     * END OF MODIFICATIONS
     * ============================================ */
    
    .book-card-content {
        padding: 2rem;
    }
    
    .book-card h3 {
        margin: 0 0 0.5rem;
        color: #1f2937;
        font-size: 1.6rem;
        font-weight: 700;
    }
    
    .book-card .author {
        color: #764ba2;
        font-weight: 600;
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
    }
    
    .book-card .price {
        color: #10b981;
        font-size: 1.5rem;
        font-weight: bold;
        margin: 0.5rem 0;
    }
    
    .book-card a {
        display: inline-block;
        margin-top: 1rem;
        padding: 0.9rem 2rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        text-decoration: none;
        border-radius: 10px;
        font-weight: 600;
        transition: all 0.3s;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 1px;
    }
    
    .book-card a:hover {
        transform: translateX(5px);
        box-shadow: 0 5px 20px rgba(102,126,234,0.4);
    }
    
    #showRanking {
        position: fixed;
        bottom: 2rem;
        right: 2rem;
        padding: 1.3rem 2.2rem;
        background: linear-gradient(135deg, #f59e0b 0%, #ef4444 100%);
        box-shadow: 
            0 10px 30px rgba(245,158,11,0.4),
            0 0 40px rgba(245,158,11,0.2);
        font-size: 1.2rem;
        animation: pulse 2s infinite;
        z-index: 1000;
        border-radius: 60px;
        font-weight: bold;
    }
    
    @keyframes pulse {
        0%, 100% { 
            transform: scale(1);
            box-shadow: 
                0 10px 30px rgba(245,158,11,0.4),
                0 0 40px rgba(245,158,11,0.2);
        }
        50% { 
            transform: scale(1.08);
            box-shadow: 
                0 15px 40px rgba(245,158,11,0.5),
                0 0 60px rgba(245,158,11,0.3);
        }
    }
    
    @media (max-width: 768px) {
        .logo-title {
            font-size: 3.5rem;
        }
        .book-icon {
            font-size: 3rem;
        }
        .subtitle {
            font-size: 1.2rem;
        }
    }
</style>



<div class="hero-section">
    <h1 class="logo-title">
        <span class="book-icon">📚</span>BookNest
    </h1>
    <div class="decorative-line"></div>
    <p class="subtitle">✨ Discover Your Next Great Read ✨</p>
</div>

<form id="searchForm" action="/books/search" method="GET">
    <input type="text" id="searchInput" name="q" placeholder="🔍 Search books, authors, or categories..." required minlength="2">
    <button type="submit">Search</button>
</form>

{{#if popularBooks}}
<h2>🔥 Popular Books</h2>
<div class="book-list">
    {{#each popularBooks}}
    <div class="book-card">
        <img src="{{this.coverUrl}}" alt="{{this.title}}" onerror="this.style.background='linear-gradient(135deg, #667eea 0%, #764ba2 100%)'">
        <div class="book-card-content">
            <h3>{{this.title}}</h3>
            <p class="author">{{this.author}}</p>
            <p>Rating: {{this.rating}} ⭐</p>
            <p class="price">${{this.price}}</p>
            <a href="/books/details?id={{this._id}}">View Details →</a>
        </div>
    </div>
    {{/each}}
</div>
{{/if}}

{{#if newBooks}}
<h2>🆕 New Releases</h2>
<div class="book-list">
    {{#each newBooks}}
    <div class="book-card">
        <img src="{{this.coverUrl}}" alt="{{this.title}}" onerror="this.style.background='linear-gradient(135deg, #667eea 0%, #764ba2 100%)'">
        <div class="book-card-content">
            <h3>{{this.title}}</h3>
            <p class="author">{{this.author}}</p>
            <p>Rating: {{this.rating}} ⭐</p>
            <p class="price">${{this.price}}</p>
            <a href="/books/details?id={{this._id}}">View Details →</a>
        </div>
    </div>
    {{/each}}
</div>
{{/if}}

<button id="showRanking" onclick="alert('🏆 Top Rankings:\n\n1. Atomic Habits - 4.8⭐\n2. The Psychology of Money - 4.7⭐\n3. Deep Work - 4.6⭐\n4. Clean Code - 4.5⭐\n5. Thinking Fast and Slow - 4.4⭐')">
    🏆 Rankings
</button>